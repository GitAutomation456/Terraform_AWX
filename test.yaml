- name: Run Terraform Plan
  hosts: 192.168.0.241
  gather_facts: no
  tasks:
    - name: Initialize Terraform
      shell: hostname
      register: out

    - name: Display Output
      debug:
        msg: "{{ out.stdout }}"

      

 
